__pycache__/
*.pyc
bin/
# D581: `make cover` writes a 2 MB profile into the tree. Untracked and unignored,
# one `git add -A` puts it in history for good.
go/cover.out
differential-failures/
.integration/
.canary/
.groundhold/
state/
# `state/` is a local runtime-state scratch dir, but the name also matches the
# go/internal/state SOURCE package — new files there were silently ignored (its
# existing files are tracked only because they were force-added). Re-include it.
!go/internal/state/
# mkdocs build output (D490: `mkdocs build --strict` now runs in CI and locally)
website/site/

# compiled CLI binary (build artifact — never commit)
/go/groundhold
/groundhold

# Go build cache — pre-existing tracked bloat (D627), never publish, never track
.gocache/
