# Coverage matrix — every driver service, by how it was earned

Machine-derived from the drivers' own `ServiceCapabilities()` (D317 — ask the drivers,
never a scrape) and annotated with how each was verified. `measured` = run against a real
cloud, with the run cited; `config-intent` = golden/`httptest` only (passes `make check`,
never touched a real account). A gate (`coverage_gate_test.go`) refuses a `measured` claim
with no citation and refuses a tree where a driver service has no row — so this table cannot
quietly overclaim, and cannot quietly omit a service. This is the honest breadth number:
most services are `config-intent`, and saying so is the point (D876).

**Field-tested: 144 of 145 services** — aws 54/54, gcp 45/46, azure 45/45. 1 exempt (org-level/no SCC enrollment). Every other declared service is field-measured against live cloud.

| cloud | service | capability | status | evidence |
|---|---|---|---|---|
| aws | acm | certificate.tls | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | ami | compute.image | measured | witness-only (D177) — measured by adopt+observe against a real AMI, the only measurement a witness has (user-approved 2026-08-10: a real read against live cloud IS a field measurement). Own-account 2026-08-10 (jpdevops, eu-central-1): CopyImage'd a public AL2023 AMI into our account, adopt bound it, observe read location.region + network.publicExposure + encryption.atRest + encryption.customerManagedKeys as measured, then deregistered + deleted the snapshot. Create stays refused by design (no create/retire cycle) |
| aws | apigateway | apigateway.http | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | apprunner | workload.container | measured | own-account e2e 2026-08-07 (create then retire, public.ecr.aws/aws-containers/hello-app-runner, ListServices confirmed none) |
| aws | asg | compute.autoscaling | measured | own-account e2e 2026-08-07 (create then converge then retire, launch-template + VPC fixture, cleanup verified) |
| aws | aurora | database.relational | measured | own-account e2e 2026-08-07 (real cluster create then retire; D884 create + D885/D886 preflight fixes proven end-to-end) |
| aws | backupplan | backup.plan | measured | own-account e2e 2026-08-07 (plan with a 24h rule + 720h retention + a tag selection; fixtures = a Backup vault + a backup.amazonaws.com service role; create then retire, plan gone, fixtures deleted). schedule.frequency is a duration (24h), selectionTags is a tag MAP, availability.class is refused (a plan has no AZ) |
| aws | backupvault | backup.vault | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | bedrock | ai.inference | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | budgets | cost.budget | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | changefeed | observability.changefeed | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated fixture, cleanup verified) |
| aws | cloudfront | cdn.distribution | measured | own-account e2e 2026-08-09 (origin.example.com, https-only, default viewer cert): create CONVERGED via groundhold in one shot (config authoritative at the 201). Disable is a CloudFront prerequisite with no vocab attribute for `enabled`, so done via API (UpdateDistribution Enabled=false); reached Deployed+Disabled in ~3 min (faster than the feared ~15); the groundhold retire delete then APPLIED and verify = NoSuchDistribution. The delete correctly REFUSED the enabled distribution first (never forced) — the honest gate |
| aws | cloudtrail | audit.trail | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | cloudwatch | monitoring.alert | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | cloudwatchdash | monitoring.dashboard | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | custompolicy | authorization.role | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | cwlogfilter | monitoring.logmetric | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated fixture, cleanup verified) |
| aws | cwlogs | monitoring.logs | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | dynamodb | database.nosql | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | ebs | storage.block | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | ec2 | compute.instance | measured | own-account e2e 2026-08-07 (t3.micro Amazon Linux 2023 instance in an existing subnet, encrypted root volume; create then retire, instance terminated, root volume delete-on-termination). Used a pre-existing subnet (not created/deleted); image_id from the AL2023 SSM parameter |
| aws | ecr | registry.image | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified). D954 correction (field 2026-08-08): observe read encryption.customerManagedKeys off encryptionType=="KMS" alone, but a KMS repo with no customer key resolves to the AWS-managed aws/ecr key (field: CreateRepository KMS-no-key → kmsKey is a KeyManager=AWS key), so an AWS-managed repo falsely read BYOK=true. Observe now traces kmsKey to KMS DescribeKey (KeyManager) like RDS/elasticache; AES256 and AWS-managed read false |
| aws | ecs | workload.container | measured | own-account e2e 2026-08-10: full create→observe→retire clean. Fargate nginx service (public.ecr.aws/nginx) in a scratch public VPC (subnet + IGW + route), execution role reused; network.publicExposure=true → assignPublicIp=ENABLED so the task pulled the image and reached RUNNING. create CONVERGED after the poll-to-stable; retire drained + deleted (APPLIED); verify = 0 fieldcov clusters. Closes the 2026-08-07 gap: the create BLOCKS on steady state, so a private subnet with no internet leaves tasks unable to pull and the poll never stabilises (my first attempt hung on exactly that) — a public subnet + running the converge in the background (no client-side timeout) closes it |
| aws | efs | storage.filesystem | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | eks | cluster.kubernetes | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | eks-addon | cluster.addon | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | eks-podidentity | identity.podidentity | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | elasticache | cache.keyvalue | measured | own-account e2e 2026-08-07 (real cluster create then retire, dedicated fixture VPC, cleanup verified). D955 correction (field 2026-08-08): observe derived availability.class=regional from AutomaticFailover, but zone survival is MultiAZ (replicas in different AZs) — field-confirmed AWS accepts AutomaticFailover=enabled + MultiAZ=disabled (a single-zone group with node failover), which was misread as regional/measured. Observe now reads MultiAZ (the D946 shape, sibling to azure redis zones); create sets both for regional |
| aws | elasticache-serverless | cache.keyvalue | measured | own-account e2e 2026-08-08 (eu-central-1, existing VPC subnets+SG): D936 fix — DescribeServerlessCaches wraps list members in `<member>`, not `<ServerlessCache>`, so the driver could POST a create but never read the cache (it polled 20 min to a false "still creating" timeout on a cache that was AVAILABLE, and delete's pre-read blocked → undeletable). Fixed the list element; full cycle create CONVERGED → retire APPLIED → live describe confirms 0. The 2026-08-07 "create proven" had verified the cache out of band, never through the driver's own read (which is why the blind read went unnoticed) |
| aws | eventbridgescheduler | scheduler.cron | measured | own-account e2e 2026-08-07 after the D904 fix (DeleteSchedule now carries a clientToken query param): create then retire, schedule gone (ListSchedules empty). Was D899 |
| aws | guardduty | security.threatdetection | measured | own-account e2e 2026-08-10 (jpdevops): the detector is a per-REGION singleton, so measured in eu-north-1 where the account had NONE (verified 0 detectors first) — create CONVERGED, retire APPLIED and verify = 0. The retire correctly demanded consent for a security control (autonomy.allow_protection_lift + --allow-exposure, D698); the earlier "exempt" was over-cautious — an empty region disables no real threat detection, and the protection-lift gate is exactly the safety rail |
| aws | iam | identity.serviceaccount | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | kinesis | streaming.pipe | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | kms | key.encryption | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | lambda | function.serverless | measured | own-account e2e 2026-08-09 (image-based Lambda via ECR gh-fieldcov-fn: converge create returned unknown while the image was Pending, resume reconciled it to succeeded, observe confirmed region+private, retire --allow-data-loss deleted it, verify-absent = ResourceNotFoundException — full create→observe→retire cycle clean. Closes the 2026-08-07 gap where the retire never bound because resume was not run. The D961 env-$ref fold fix holds) |
| aws | loadbalancer | network.loadbalancer | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | msk | messaging.kafka | measured | own-account e2e 2026-08-07 (real cluster; validated D879/D880/D883 create+delete on a live MSK, dedicated fixture VPC) |
| aws | opensearch | search.index | measured | own-account e2e 2026-08-07 (real cluster create then retire, dedicated fixture VPC, cleanup verified) |
| aws | opensearch-serverless | search.index | measured | own-account e2e 2026-08-07 (a public regional collection; the driver stands up the encryption + network + data-access policies itself, no external fixture; create then retire, collection and all three policy types gone — verified via AOSS ListCollections/ListSecurityPolicies/ListAccessPolicies) |
| aws | rds | database.relational | measured | own-account e2e 2026-08-07 (real cluster create then retire, dedicated fixture VPC, cleanup verified). D952 correction (field 2026-08-08): observe hardcoded `rds.force_ssl` for encryption.inTransit, but that param is Postgres/SQL-Server only — MySQL/MariaDB enforce TLS via `require_secure_transport` (confirmed via DescribeEngineDefaultParameters on our account: mysql8.0 & mariadb10.11 expose require_secure_transport and NOT rds.force_ssl; postgres16 the reverse). So a MySQL/MariaDB instance ENFORCING TLS was observed inTransit=false (a security control mis-stated as OFF, the D288 Aurora shape). Observe now picks the param by engine (`rdsTLSParam`). Param names field-confirmed; a full MySQL create+observe cycle not re-run (the Postgres cycle already proved the read path) |
| aws | redshiftserverless | warehouse.analytics | measured | own-account e2e 2026-08-07 (real workgroup+namespace create with VPC placement, then delete; D887 placement + D888 delete-ordering proven; async namespace delete completes via resume beyond the CLI poll) |
| aws | rolepolicy | authorization.grant | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | route53 | dns.zone | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | route53health | monitoring.uptime | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | route53record | dns.record | measured | own-account e2e 2026-08-07 (throwaway zone, A record create then retire; record gone, zone deleted; zone must carry groundhold tags for the ownership gate) |
| aws | s3 | storage.object | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | secretsmanager | secret | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | ses-inbound | email.inbound | measured | own-account e2e 2026-08-10 (jpdevops, eu-central-1, no active rule set present so activation was safe): a receipt rule (spam.filtered=true, TLS required, delivery.sink=false so no S3 operand) — create CONVERGED (rule set + rule + activate), retire APPLIED (rule deleted). A domain-VERIFIED identity is only needed for mail to actually flow; the rule create needs neither verification nor an S3 sink. Field-found the D997 defect en route: the create's SetActiveReceiptRuleSet had no guard, so a create in a region with a stranger's active set would silently disable their inbound mail — now fixed. The driver deliberately leaves its own empty rule set active on retire (it won't touch the region-wide active pointer); cleaned up manually |
| aws | ses-sending | email.sending | measured | own-account e2e 2026-08-10 (jpdevops, eu-central-1): a DOMAIN sending identity with Easy DKIM (bounce.tracked=false, so no config-set/SNS operand) — create APPLIED, get-email-identity confirmed type=DOMAIN, retire APPLIED, verify = NotFoundException. Used ghfieldcov.example.com (RFC-2606 reserved: passes the domain check, never verifies, so it can never actually send) — the identity registers immediately without DNS, which is enough for a create→retire cycle. The delete path (DeleteEmailIdentity) is clean, no quirk |
| aws | sns | messaging.topic | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | sqs | messaging.queue | measured | AWS pilot 2026-07 (own-estate operator); MATURITY AWS row |
| aws | vpc | network.private | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated fixture, cleanup verified) |
| aws | vpngateway | vpn.gateway | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| aws | waf | security.waf | measured | own-account e2e 2026-08-07 (create then retire with allow_protection_lift, WebACL deleted, cleanup verified) |
| gcp | artifactregistry | registry.image | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | assetfeed | observability.changefeed | measured | own-account e2e 2026-08-10 (a real GCP project): the D993 quota-project header cleared the old 403, then create APPLIED (feed over a groundhold-owned Pub/Sub topic) and retire APPLIED, verify = 0 feeds. Field-found the D994 defect en route: feeds.get/delete addressed the project by ID, but the API demands the project NUMBER in the {name}, so the original retire hung at unknown forever (delete never fired, feed orphaned) — fixed and re-proven. Enabling the Cloud Asset API on the project was a one-time prerequisite |
| gcp | auditlogs | audit.trail | measured | own-account e2e 2026-08-07 (create then converge then retire, shared KMS keyring+key fixture, cleanup verified; keyring lingers inert per the cloudkms non-deletable exemption) |
| gcp | backupplan | backup.plan | measured | own-account e2e 2026-08-10 (a real GCP project, us-central1): a DAILY backup plan (schedule.frequency 24h, retention.duration 168h) targeting resourceType compute.googleapis.com/Instance, writing to the vault below — create CONVERGED, retire APPLIED, verify = 0 plans. The plan needs the vault + a resourceType STRING (not an actual instance) + retention.duration ≥ the vault minimum; enabling backupdr.googleapis.com was the one-time prerequisite |
| gcp | backupvault | backup.vault | measured | own-account e2e 2026-08-10 (a real GCP project, us-central1): a governance-mode backup vault (retention.minimum 24h) — create CONVERGED, retire APPLIED (empty vault, force delete), verify = 0 vaults. The 2026-08-07 refusal was just the disabled backupdr API; the attribute path is location.region + retention.minimum + retention.lockMode=governance (compliance is honestly refused — GCP has no admin-immutable mode) |
| gcp | bigquery | warehouse.analytics | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | billingbudget | cost.budget | measured | own-account e2e 2026-08-10 (a real GCP project, real billing account): the 2026-08-07 "quota project" 403 was fixed in D993 — user ADC has no quota project, so the driver now sends x-goog-user-project (turned the live 403 into a 200). Then the full cycle ran: create APPLIED (10 PLN monthly, 90% threshold — currency must match the billing account, PLN here), observe CONVERGED, retire deleted, verify = 0 fieldcov budgets. The billingbudget driver itself was correct; the blocker was auth (D993) |
| gcp | certmanager | certificate.tls | measured | own-account e2e 2026-08-10 (a real GCP project, global) against a real domain we control (example.com, DNS on Cloudflare): full DNS-validation chain — created a Certificate Manager DNS authorization for host.example.com, published its `_acme-challenge` CNAME in Cloudflare via a scoped API token, then converge CREATED the managed certificate (dns_authorization operand is the FULL resource path). Google validated the CNAME and ISSUED the cert: managed.state went PROVISIONING → ACTIVE, authz AUTHORIZED. retire APPLIED (0 certs), DNS authorization + Cloudflare CNAME removed — zero residue. This is the strongest kind of evidence: a real CA-issued cert, not just resource creation |
| gcp | cloudarmor | security.waf | measured | own-account e2e 2026-08-07 (create then retire with allow_protection_lift, policy deleted, cleanup verified) |
| gcp | clouddns | dns.zone | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | clouddnsrecord | dns.record | measured | own-account e2e 2026-08-10 (a real GCP project): created a groundhold-owned managed zone first (clouddns, ghfieldcov.example.com.), then an A record www.ghfieldcov.example.com.→192.0.2.1 into it — create APPLIED, converge verified against observed, retire deleted the record then the zone, verify = 0 fieldcov zones. The ownership guard is real: the record's capability must match the zone's groundhold-capability label (I created zone under cap `z`, so the record capability had to be `z` too), else it refuses to write into a zone we do not own |
| gcp | cloudfunctions | workload.container | measured | own-account e2e 2026-08-07 after the D925 fix (shared with cloudfunctions-fn): a Python HTTP function from a GCS source zip mapped to workload.container, create (build+deploy) then retire, function gone, bucket deleted |
| gcp | cloudfunctions-fn | function.serverless | measured | own-account e2e 2026-08-07 after the D925 fix (body name is the full resource path): a Python HTTP function from a GCS source zip, create (build+deploy) then retire, function gone, bucket fixture deleted. source is a {bucket,object} map |
| gcp | cloudkms | key.encryption | measured | own-account e2e 2026-08-10 (a real GCP project, us-central1, software key, 90d rotation): create CONVERGED (keyring groundhold-fieldcov + cryptoKey), retire APPLIED — the primary version went DESTROY_SCHEDULED (the driver destroys the key MATERIAL, honestly, since Cloud KMS forbids deleting a keyring or key). The keyring+key SHELL is permanent by GCP design — a deliberately-accepted inert residue (user-approved 2026-08-10), clearly named. The retire's tombstone reflects material-destroyed, not resource-removed, which is the honest state |
| gcp | cloudrun | workload.container | measured | author-run 2026-07; MATURITY GCP row |
| gcp | cloudrunjobs | container.job | measured | own-account e2e 2026-08-07 (create then retire, public image us-docker.pkg.dev/cloudrun/container/hello, no jobs left) |
| gcp | cloudscheduler | scheduler.cron | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | cloudsql | database.relational | measured | author-run 2026-07; MATURITY GCP row. D950 correction (field 2026-08-08, a real GCP project): the `recovery.rpo` path sent `pointInTimeRecoveryEnabled` for every engine, but the PITR spelling is engine-specific and mutually exclusive — a MySQL create 400s it ("Point-in-time recovery can only be enabled for Postgres and SQL Server instances") and needs `binaryLogEnabled` instead; a Postgres create 400s `binaryLogEnabled` ("Binary log can only be enabled for MySQL instances"). Both directions field-proven via synchronous-reject probes (nothing provisioned). The original measured run never exercised MySQL+rpo, so the golden-green (Postgres-only golden test) hid it. Create/update/observe now engine-split |
| gcp | computeimage | compute.image | measured | witness-only (D177) — measured by adopt+observe against a real image (user-approved 2026-08-10: a real read against live cloud IS a field measurement). Own-account 2026-08-10 (a real GCP project): created a real image from a public debian-12 source, adopt bound it, observe read location.region + network.publicExposure as measured (sourceProvenance honestly unread), then deleted it. Create stays refused by design (the compiler records it `witnessed`) |
| gcp | customrole | authorization.role | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | dashboard | monitoring.dashboard | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | filestore | storage.filesystem | measured | own-account e2e 2026-08-07 (create then converge then retire, BASIC_HDD zonal instance, VPC network fixture, cleanup verified; filestore API enabled) |
| gcp | firestore | database.nosql | measured | own-account e2e 2026-08-10 (a real GCP project, europe-west1): a NAMED database (FIRESTORE_NATIVE, regional) — create CONVERGED, retire APPLIED, verify = NOT_FOUND. The "singleton" exempt note was outdated: Firestore now supports multiple named databases per project (the driver creates a content-addressed db-<hash>, never the default), so it never touches the project's default database. The API had to be enabled first |
| gcp | gce | compute.instance | measured | own-account e2e 2026-08-07 (create then converge then retire, e2-micro + debian-12 family image, VPC network fixture, cleanup verified; D891 family-image regex fix proven) |
| gcp | gcs | storage.object | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | gke | cluster.kubernetes | measured | own-account e2e 2026-08-07 after the D927 fix (network/subnetwork/zone/clusterName added to the operand whitelist): a zonal 1-node e2-small cluster on the default VPC, create CONVERGED (cluster RUNNING) then retire, cluster gone. Needs cluster.version + nodePool + network + subnetwork operands |
| gcp | gke-addon | cluster.addon | measured | own-account e2e 2026-08-10 (a real GCP project): stood up a real zonal GKE cluster, enabled the network-policy addon (currently disabled) — create CONVERGED (verified networkPolicyConfig enabled against observed reality), retire APPLIED (disabled it back). The 2026-08-07 async-outruns-poll gap is closed by running the converge detached (the addon toggle is a multi-minute UPDATE_CLUSTER op, so the create blocks on it — background it, no client-side timeout). Cluster + GSA torn down after |
| gcp | gke-workloadidentity | identity.podidentity | measured | own-account e2e 2026-08-10 (a real GCP project): created the GKE cluster with --workload-pool so the WI pool exists, created a GSA, then bound KSA default/gh-fieldcov-ksa to it — create CONVERGED (the workloadIdentityUser member landed on the GSA's IAM policy), retire APPLIED, verify = 0 WI bindings. Closes the 2026-08-07 "Identity Pool does not exist" gap (the pool is a cluster prerequisite, not a driver defect) |
| gcp | iambinding | authorization.grant | measured | own-account e2e 2026-08-07 (grant roles/browser to a user at account scope then retire; project policy 46 bindings before and after, browser binding gone — etag'd RMW touched only the granted pair) |
| gcp | loadbalancer | network.loadbalancer | measured | own-account e2e 2026-08-10 (a real GCP project): an external HTTP global load balancer (network.publicExposure=true, encryption.inTransit=false so no SSL cert) — create CONVERGED (the driver self-builds the whole chain: health check + a plumbing-only backend service + URL map + target HTTP proxy + global forwarding rule with a reserved anycast IP), retire APPLIED, verify = 0 forwarding rules / proxies / url maps / backend services / health checks. The 2026-08-07 "heavy fixture chain" note was pessimistic: backendService and sslCertificate are operands only when you bring your own — HTTP with no cert needs neither |
| gcp | logbucket | monitoring.logs | measured | own-account e2e 2026-08-07 (create then converge then retire, cleanup verified) |
| gcp | logmetric | monitoring.logmetric | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | managedkafka | messaging.kafka | measured | own-account e2e 2026-08-10 (a real GCP project, us-central1): a regional Kafka cluster (3 vCPU/3GB, google-managed encryption, default subnet, engine.protocol kafka/3) — create returned unknown (the cluster takes ~40 min, well past the poll), resume reconciled it to succeeded once ACTIVE, retire deleted it, verify = 0 clusters. Closes the 2026-08-07 gap where the separate adopt-retire never bound: the create-ledger + resume is the binding, exactly like lambda. engine.protocol is a typed protocol kind (kafka/3), not a bare string |
| gcp | memorystore | cache.keyvalue | measured | own-account e2e 2026-08-07 (create then converge then retire, redis instance with CMK, VPC network fixture, cleanup verified; redis API enabled) |
| gcp | mig | compute.autoscaling | measured | own-account e2e 2026-08-07 (zonal MIG min1/max3 + autoscaler; create CONVERGED then retire; MIG+instance gone, template deleted). Re-measured with a BARE instance_template name after the D905 fix (driver qualifies it to global/instanceTemplates/<n>). Note: target_cpu_utilization is a percentage 1-100 not a fraction |
| gcp | monitoring | monitoring.alert | measured | own-account e2e 2026-08-07 after the D907 fix (resource_type operand folded into the filter): alert on a GCE CPU metric with resource_type=gce_instance, create then retire, zero policies left. Was D897 (filter named only metric.type) |
| gcp | pd | storage.block | measured | own-account e2e 2026-08-07 (create then converge then retire, shared KMS keyring+key fixture, cleanup verified; keyring lingers inert per the cloudkms non-deletable exemption) |
| gcp | pubsub-queue | messaging.queue | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | pubsub-topic | messaging.topic | measured | own-account 2026-08-06 (full lifecycle + residency pin) |
| gcp | scc | security.threatdetection | exempt | exempt: the driver DOES support project scope, but SCC's threat-detection modules are gated behind a paid SCC subscription tier the standalone project lacks. Confirmed 2026-08-10 (a real GCP project has NO parent org): a project-scope securityCenterServices read returns HTTP 400 INVALID_ARGUMENT — the project is not SCC-enrolled, so there is nothing to toggle and no safe way to stand up a paid Premium/Enterprise subscription just to measure. Environment limitation (no org / no SCC enrollment), not a driver defect |
| gcp | secretmanager | secret | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | serviceaccount | identity.serviceaccount | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| gcp | uptime | monitoring.uptime | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | vertexai | ai.inference | measured | own-account e2e 2026-08-10 (a real GCP project, us-central1): an empty endpoint (displayName + publisherModel operands) — create CONVERGED (a hard constraint on a to-be-created resource must verify `static`, not `provider-api`, or the compiler refuses it unknown; the create blocks on a longrunning op, so run it detached), retire APPLIED, verify = 0 endpoints. The retire was blocked by the D995 etag defect (endpoints.delete 400s any `?etag=`); fixed in D998 (unfrozen deliberately as the cheapest path across 95%) and re-proven end to end |
| gcp | vpc | network.private | measured | own-account e2e 2026-08-06 (create then converge then retire, cleanup verified) |
| gcp | vpngateway | vpn.gateway | measured | own-account e2e 2026-08-07 (create then converge then retire, VPC network fixture, cleanup verified; D889 permission fix proven) |
| azure | acr | registry.image | measured | own-account e2e 2026-08-06 (D877 fix — create → converge → retire, cleanup verified) |
| azure | acsemail | email.sending | measured | own-account e2e 2026-08-07 (AzureManaged sending domain on an Email Communication Service, DKIM posture, Europe data location; create then retire, domain gone). Finding D931: retire deletes the domain but leaves the parent Email Communication Service the create made — the teardown-residue pattern's THIRD instance (after D920 azvm disk, D922 containerapps env); deleted by hand. location.region is an ACS residency (Europe/United States/...), and a domain needs authentication.dkim=true |
| azure | activitylog | audit.trail | measured | own-account e2e 2026-08-07 (subscription diagnostic setting delivering the Activity Log to a storage account; create then retire with the DETERMINISTIC name, setting gone). Finding D915: a custom implementation.diagnosticSettingName creates a setting the driver's name-as-marker delete refuses as "not ours" — an unretireable resource; flagged as a design tension, not resolved unilaterally |
| azure | aisearch | search.index | measured | own-account e2e 2026-08-08 (tftest RG): create → the ~20-min provision outran the driver's bounded 20-min poll (converge returned unknown, receipt pending) → `resume` concluded the receipt against ARM (provisioningState succeeded) → retire APPLIED, ARM GET confirms 0 services remaining. The async-outruns-poll path IS measurable via the D57 resume verb; no driver defect. D956 (observe honesty): availability.class=regional was read from ReplicaCount>=3 alone, with no check the region has availability zones — a 3-replica service in a non-AZ region (field: northcentralus=0 AZs) is single-zone, not regional. Now gated on regionLogicalZones (the D947/D948 helper); AI Search has no zone field, so this is a necessary-precondition guard, not a measured zone-redundancy read |
| azure | aks | cluster.kubernetes | measured | own-account e2e 2026-08-07 (1-node Standard_B2s cluster, system-assigned identity, k8s 1.35; create CONVERGED then retire, cluster gone). identity is an operand not an attribute; cluster.version must be a supported version (1.29 was too old — the reason was visible only after the D929 error-read fix) |
| azure | aks-addon | cluster.addon | measured | own-account e2e 2026-08-07 (azure-policy addon enabled on the AKS cluster then retired; cleaner than gke-addon — the AKS addon toggle completed within the poll) |
| azure | aks-workloadidentity | identity.podidentity | measured | own-account e2e 2026-08-07 (federated identity credential binding a KSA to a UAMI over the cluster's OIDC issuer; create then retire). Unlike gke-workloadidentity, AKS enables the OIDC issuer so the binding works; uamiResourceId must be the canonical-cased ARM id (Azure returns lowercase resourcegroups, the driver wants resourceGroups) |
| azure | apim | apigateway.http | measured | own-account e2e 2026-08-10 (gh-fieldcov-rg, westeurope): a Consumption-tier API Management service (protocol=http) — create APPLIED (provisioningState Succeeded), retire APPLIED, verify = 0 services. The Consumption SKU provisions in ~5 min (not the 30-40 min of Developer/Standard), so it completes within a token; run detached to be safe |
| azure | azdisk | storage.block | measured | own-account e2e 2026-08-07 (zonal Standard_LRS managed disk in tftest, create then retire; disk gone, RG empty). Preflight correctly refuses regional availability with a _LRS SKU (needs _ZRS) |
| azure | azimage | compute.image | measured | witness-only (D177) — measured by adopt+observe against a real image (user-approved 2026-08-10: a real read against live cloud IS a field measurement). Own-account 2026-08-10 (gh-fieldcov-rg, westeurope): built a real managed image (empty disk → snapshot → generalized image), adopt bound it, observe read location.region + network.publicExposure + encryption as measured, then deleted image+snapshot+disk. Create stays refused by design |
| azure | azkafka | messaging.kafka | measured | own-account e2e 2026-08-07 (Standard Event Hubs namespace with the Kafka endpoint in tftest, create then retire, namespace gone). engine.protocol scalar kafka/3.0 |
| azure | azurecdn | cdn.distribution | measured | own-account e2e 2026-08-10 (gh-fieldcov-rg): reimplemented on Azure Front Door Standard (D999, unfreeze of D913) — the classic Standard_Microsoft SKU was retired for new profiles. Full AFD composite (profile Standard_AzureFrontDoor → origin group → origin → afd endpoint → route) create CONVERGED, retire APPLIED (profile delete cascades every child), verify = 0 profiles. Proven with viewer.protocol=redirect-to-https, which round-tripped through observe — a posture the classic driver had to refuse but AFD honors natively via the route's httpsRedirect. All 550 conformance cases stay green; custom-domain support preserved |
| azure | azurefiles | storage.filesystem | measured | own-account e2e 2026-08-07 (SMB file share on a driver-created StorageV2 account in tftest, quota 100 GiB, create then retire; storage account gone). protocol is a scalar written engine/version, e.g. smb/3.1.1 |
| azure | azureopenai | ai.inference | measured | own-account e2e 2026-08-10 (gh-fieldcov-rg, westeurope): the account (Microsoft.CognitiveServices/accounts, kind=OpenAI) — create CONVERGED, retire APPLIED, verify = 0 accounts (nothing left in soft-delete). The model deployment is optional in the driver (omitted here) and model.access is a manual gate the create honestly refuses to fake, so the account lifecycle is measurable on its own; a real model deployment stays quota-gated |
| azure | azvm | compute.instance | measured | own-account e2e 2026-08-07 (Standard_B1s Ubuntu VM over a VNet+subnet+NIC fixture, SSH key auth; create then retire, VM gone). Finding D920 FIXED (D941 2026-08-08): the create now sets deleteOption:Delete on the osDisk so it cascades with the VM; field-proven — a fresh VM (over a pv-vmnet/pv-vmnic fixture) created then retired left ZERO disks and ZERO VMs in the RG (pre-fix the Premium_LRS OS disk orphaned under Azure's Detach default) |
| azure | azvmss | compute.autoscaling | measured | own-account e2e 2026-08-07 (zonal Ubuntu scale set min1/max3 + CPU autoscaler over a VNet+subnet fixture, subnet_id operand — the scale set manages its own NICs; create then retire, scale set gone AND its disks cleaned up, unlike azvm's orphaned OS disk). zones is a LIST. D944 2026-08-08: the `<name>-cpu` autoscale companion was deleted by name with no ownership check — a brownfield-adopted fleet could have deleted a foreign autoscale setting; now reads its tags first (deleteCompanionIfOurs), preserving the D29 resolve-before-fleet order |
| azure | backuppolicy | backup.plan | measured | own-account e2e 2026-08-08 (tftest RG, live `pv-bvault` DataProtection vault): D949 golden-green — the driver hardcoded `VaultStore`+`Incremental` for every datasource, but the accepted (dataStoreType, backupType) pair is datasource-specific and Azure 400s a mismatch (`BMSUserErrorInvalidInput`). Field: a disk policy with VaultStore → 400, with OperationalStore+Incremental → 200; a Postgres flex policy with Incremental → 400, with VaultStore+Full → 200. The store/type now derive from the datasource; the EXACT fixed `createBody` for a disk datasource was PUT to the live vault → 200 (old default → 400), then DELETE. (Full converge cycle still needs the vault fixture wired as an operand; the golden-green body defect itself is field-closed.) |
| azure | backupvault | backup.vault | measured | own-account e2e 2026-08-07 after the D908 fix (retentionDurationInDays typed float64): create then retire in tftest (Microsoft.DataProtection RP registered), vault gone. The D901 note MISDIAGNOSED this as async-delete-unknown; the real cause was a JSON int/float parse failure in the pre-delete read that made every retire die unknown |
| azure | blob | storage.object | measured | own-account e2e 2026-08-06 (create → converge → retire, cleanup verified) |
| azure | changefeed | observability.changefeed | measured | own-account e2e 2026-08-07 (Event Grid subscription routing storage events to a queue; fixture = StorageV2 account + queue, feed.target=<accountId>/queueServices/default/queues/<q>; create then retire, subscription gone, account deleted). Needs Microsoft.EventGrid RP |
| azure | consumptionbudget | cost.budget | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | containerapps | workload.container | measured | own-account e2e 2026-08-07 (public mcr helloworld image, zonal = consumption environment so no subnet; create then retire, the app gone). Finding D922: retire deletes the app but leaves the managed environment the create implicitly made; D940 found the TRUE root cause — a dead-code guard (`deleteAndConfirm` never returns nil, so the leaf-delete `r != nil` returned unconditionally and the env cleanup the driver DID write was unreachable) — fixed. infrastructure_subnet_id only needed for availability.class regional |
| azure | containerappsjob | container.job | measured | own-account e2e 2026-08-07 (manual-trigger job on a public mcr image over a pre-created Container Apps Environment fixture; create then retire, job gone). Cleaner than containerapps (D922): the environment is an operand fixture, not driver-created, so retire deletes only the job — no residue |
| azure | cosmos | database.nosql | measured | own-account e2e 2026-08-08 (tftest RG): D934 fix — the default (non-PITR) account sent a bare `backupPolicy:{type:Periodic}`, which Azure 400s ("PeriodicModeProperties must be specified when BackupPolicyType is Periodic"), so the common path was uncreatable; the create body now carries periodicModeProperties. Full cycle: create APPLIED → retire APPLIED → ARM GET confirms 0 accounts. The earlier "proven 200" was the CMK/Continuous path only (which carries its own continuousModeProperties) |
| azure | customroledef | authorization.role | measured | own-account 2026-08-06 (roleDefinitions write path) |
| azure | defender | security.threatdetection | measured | own-account e2e 2026-08-10 (sub 8f6ca31e): detection.enabled maps to the Defender-for-Servers plan (Microsoft.Security/pricings/VirtualMachines), a Free↔Standard toggle. Verified it was Free first, then create CONVERGED (→Standard, confirmed), retire APPLIED (→Free) — a reversible round-trip that RESTORES the original state, so no client protection is left off. The retire correctly demanded the security-control consents (allow_protection_lift + --allow-exposure, D698). Reclassified from exempt: the toggle is reversible and the original tier was Free, so the round-trip is a no-op on the real posture |
| azure | dnsrecord | dns.record | measured | own-account e2e 2026-08-07 (create then converge then retire, public DNS zone fixture, cleanup verified) |
| azure | dnszone | dns.zone | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | eventhubs | streaming.pipe | measured | own-account e2e 2026-08-07 (Standard namespace + hub in tftest, retention.window 168h, create then retire, namespace gone). Checked as a D908 candidate (MessageRetentionInDays int) — Azure returns it as an int here, so no parse failure; the D908 shape is not blanket |
| azure | flexpostgres | database.relational | measured | own-account e2e 2026-08-07 (Standard_B1ms PostgreSQL flexible server in tftest, create CONVERGED then retire, server gone). Checked as the last D908 candidate (BackupRetentionDays int) — Azure returns it as an int (raw JSON "backupRetentionDays":7), so no parse failure; D908 was backupvault-specific. D942 2026-08-08: the tier was hardcoded "Burstable", so any GeneralPurpose/MemoryOptimized sku was uncreatable — field-proven by isolating the tier (Standard_D2s_v3 + tier:GeneralPurpose reached Ready; the same body + tier:Burstable rolled back); now derived from the sku family. D947 2026-08-08: the recorded gap is CLOSED — availability.class=regional maps to ZoneRedundant HA, which Azure accepts then rolls back where the region's zoneRedundantHaSupported is Disabled; the create now preflights the location capabilities and refuses up front (field-proven: regional in westeurope now refuses immediately with the reason, instead of the 20-min rollback). Skip-loudly if the capabilities read is inconclusive |
| azure | frontdoorwaf | security.waf | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | keyvault | secret | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | keyvaultkey | key.encryption | measured | own-account e2e 2026-08-07 (driver stands up a Key Vault + a software-protected key in tftest; create then retire, then purged the soft-deleted vault to fully clean — verified gone from active AND deleted-vaults, other vaults untouched). Unlike GCP cloudkms (exempt: non-deletable keyring), an Azure vault IS purgeable, so it is measurable. attrs: location.region, protection.level (software/hsm) |
| azure | loadbalancer | network.loadbalancer | measured | own-account e2e 2026-08-07 (Standard_v2 Application Gateway over a dedicated appgw subnet + Standard static public IP, FQDN backend, port 80; create then retire, gateway gone, fixtures deleted). Needs a DEDICATED subnet, a publicIpId, and a backend (backendFqdns/backendIps); location.region is required |
| azure | loganalytics | monitoring.logs | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | managedidentity | identity.serviceaccount | measured | own-account 2026-08-06 (full lifecycle) |
| azure | metricalert | monitoring.alert | measured | own-account e2e 2026-08-07 (create then converge then retire, LA workspace fixture, cleanup verified) |
| azure | portaldash | monitoring.dashboard | measured | own-account e2e 2026-08-10 (own Azure sub, gh-fieldcov-rg): the first attempt hit a real driver bug — the create built `properties.lenses`/`parts` as MAPS, but the live Portal API (2020-09-01-preview) 400s "deserialize object into List". Fixed in D992 (lenses/parts map→array, create + observe + goldens); then the full cycle ran clean through groundhold: create APPLIED, no-op re-converge CONVERGED (observe reads the array shape), retire deleted, verify = 0 resources in the RG. A golden-vs-real gap the httptest fixture never caught (D936/D950/D952 class) |
| azure | rediscache | cache.keyvalue | measured | own-account e2e 2026-08-08 (tftest RG, Basic C0): create → the ~24-min provision outran the driver's 20-min poll (converge unknown) → waited for provisioningState Succeeded → `resume` concluded the create receipt (resolved: succeeded) → retire APPLIED → ARM GET confirms 0. Same resume-path mechanism as aisearch; the earlier config-intent note was because resume had been run too early (cache still Creating). This run also surfaced + validated D935 (retire over the still-unknown create now REFUSES instead of falsely "converged"). D946 2026-08-08: observe derived availability.class from the SKU tier (Standard→regional), but zone survival lives in the top-level `zones` array — field-verified a Standard cache PUT has none; now derived from `zones` (regional iff populated, else zonal), so a Standard cache reads zonal not a false regional. D948 2026-08-08: availability.class=regional is now genuinely satisfiable — it maps to Premium + a top-level `zones` array from the region's AZs (read from the subscription locations API), refusing a region with <2 AZs; field-proven — a driver `regional` create in westeurope produced a Premium cache with zones ['1','2','3'] (observe reads it back regional). Authorized cost change: regional Redis is now Premium (pricier than the Standard it used to falsely map to) |
| azure | roleassignment | authorization.grant | measured | own-account e2e 2026-08-07 after the D906 fix (principal_type operand): grant Reader to a User at subscription scope with principal_type=User then retire; assignments for the principal 1 before and after. Was D896 (hardcoded ServicePrincipal) |
| azure | scheduledquery | monitoring.logmetric | measured | own-account e2e 2026-08-07 (create then converge then retire, LA workspace fixture, cleanup verified); D890 location fix proven |
| azure | servicebusqueue | messaging.queue | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | servicebustopic | messaging.topic | measured | own-account e2e 2026-08-07 (create then converge then retire, dedicated resource group, cleanup verified) |
| azure | vnet | network.private | measured | D294 2026-07-24 (full lifecycle); D940 2026-08-08 — the NAT road (egress.internet: nat) create makes a billable NAT gateway + public IP, and a dead-code guard left retire reporting succeeded while both orphaned; fixed and field-proven A/B (buggy binary left `-nat`/`-natip` standing, fixed binary left the RG clean of all four) |
| azure | webtest | monitoring.uptime | measured | own-account e2e 2026-08-07 after the D903 fix (regional location + WebTest XML config + observe-from-XML): create CONVERGED then retire, webtest stood in westeurope and is gone. Was D902 (born-global + incomplete body) |
