apiVersion: contract/v0.1
kind: InfrastructureContract
meta: { id: mcp-t, environment: test, version: 1 }
capabilities:
  - id: db
    type: capability.database.relational
constraints:
  hard:
    - id: c-managed
      subject: db
      path: service.managed
      op: equals
      value: true
      verify: { method: static }
